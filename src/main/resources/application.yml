spring:
  datasource:
    url: jdbc:mysql://localhost:3306/debate_db
    username: root
    password: M(sjs837483)
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
